provin.network.auth {
  # Base URL of the o3co auth.policy-verifier (the authorization decision point).
  # Must carry an explicit http:// or https:// scheme. No default — the server
  # fails to boot until an operator sets it, so authorization is never silently
  # evaluated against nothing.
  policy-verifier-url = ""
}
