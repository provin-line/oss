// dplaax.vc.v1 is the provenance-chain VC resolver: pipeline runtimes submit
// Verifiable Credentials, which are stored by content address and served back
// for audit and for cross-registry chain resolution.
//
// Message-shape principle (mirrors dplaax.did.v1 / dplaax.schema.v1): a VC
// crosses the wire as opaque bytes carrying its canonical (JCS) JSON — the proto
// does not re-model it. The VC body's canonical round-trip IS its content
// address ("sha256:<hex>" over the JCS-canonical body), the same address a
// previousCredential link references and an auditor recomputes, so a proto
// re-model would be a second, drift-prone source of truth.
//
// ResolveVC is the cross-registry fetch contract: a peer registry's resolver
// calls it to fill a previousCredential hole in a chain it is auditing (the
// async batch resolver is a later slice; the wire it depends on is frozen here).

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: dplaax/vc/v1/vc.proto

package vcpbconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/provin-line/oss/gen/go/dplaax/vc/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// VCResolverServiceName is the fully-qualified name of the VCResolverService service.
	VCResolverServiceName = "dplaax.vc.v1.VCResolverService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// VCResolverServiceStoreVCProcedure is the fully-qualified name of the VCResolverService's StoreVC
	// RPC.
	VCResolverServiceStoreVCProcedure = "/dplaax.vc.v1.VCResolverService/StoreVC"
	// VCResolverServiceResolveVCProcedure is the fully-qualified name of the VCResolverService's
	// ResolveVC RPC.
	VCResolverServiceResolveVCProcedure = "/dplaax.vc.v1.VCResolverService/ResolveVC"
)

// VCResolverServiceClient is a client for the dplaax.vc.v1.VCResolverService service.
type VCResolverServiceClient interface {
	// StoreVC stores a submitted VC keyed by its content address, which the server
	// RECOMPUTES from the submitted bytes (never a caller-supplied key). When the
	// VC references a previousCredential the server does not already hold, that
	// hole is enqueued as unresolved for the (later) batch resolver; the optional
	// upstream_endpoint is the caller's hint for where the predecessor can be
	// fetched. Storing a VC already held is idempotent.
	StoreVC(context.Context, *connect.Request[v1.StoreVCRequest]) (*connect.Response[v1.StoreVCResponse], error)
	// ResolveVC returns the VC held at a content address, or NotFound. A VC body
	// is provenance data, not an open identity document, so this read is L1-gated
	// (unlike W3C DID resolution). It is also the cross-registry fetch a peer
	// resolver calls; how that peer authenticates is a deployment concern.
	ResolveVC(context.Context, *connect.Request[v1.ResolveVCRequest]) (*connect.Response[v1.ResolveVCResponse], error)
}

// NewVCResolverServiceClient constructs a client for the dplaax.vc.v1.VCResolverService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewVCResolverServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) VCResolverServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	vCResolverServiceMethods := v1.File_dplaax_vc_v1_vc_proto.Services().ByName("VCResolverService").Methods()
	return &vCResolverServiceClient{
		storeVC: connect.NewClient[v1.StoreVCRequest, v1.StoreVCResponse](
			httpClient,
			baseURL+VCResolverServiceStoreVCProcedure,
			connect.WithSchema(vCResolverServiceMethods.ByName("StoreVC")),
			connect.WithClientOptions(opts...),
		),
		resolveVC: connect.NewClient[v1.ResolveVCRequest, v1.ResolveVCResponse](
			httpClient,
			baseURL+VCResolverServiceResolveVCProcedure,
			connect.WithSchema(vCResolverServiceMethods.ByName("ResolveVC")),
			connect.WithClientOptions(opts...),
		),
	}
}

// vCResolverServiceClient implements VCResolverServiceClient.
type vCResolverServiceClient struct {
	storeVC   *connect.Client[v1.StoreVCRequest, v1.StoreVCResponse]
	resolveVC *connect.Client[v1.ResolveVCRequest, v1.ResolveVCResponse]
}

// StoreVC calls dplaax.vc.v1.VCResolverService.StoreVC.
func (c *vCResolverServiceClient) StoreVC(ctx context.Context, req *connect.Request[v1.StoreVCRequest]) (*connect.Response[v1.StoreVCResponse], error) {
	return c.storeVC.CallUnary(ctx, req)
}

// ResolveVC calls dplaax.vc.v1.VCResolverService.ResolveVC.
func (c *vCResolverServiceClient) ResolveVC(ctx context.Context, req *connect.Request[v1.ResolveVCRequest]) (*connect.Response[v1.ResolveVCResponse], error) {
	return c.resolveVC.CallUnary(ctx, req)
}

// VCResolverServiceHandler is an implementation of the dplaax.vc.v1.VCResolverService service.
type VCResolverServiceHandler interface {
	// StoreVC stores a submitted VC keyed by its content address, which the server
	// RECOMPUTES from the submitted bytes (never a caller-supplied key). When the
	// VC references a previousCredential the server does not already hold, that
	// hole is enqueued as unresolved for the (later) batch resolver; the optional
	// upstream_endpoint is the caller's hint for where the predecessor can be
	// fetched. Storing a VC already held is idempotent.
	StoreVC(context.Context, *connect.Request[v1.StoreVCRequest]) (*connect.Response[v1.StoreVCResponse], error)
	// ResolveVC returns the VC held at a content address, or NotFound. A VC body
	// is provenance data, not an open identity document, so this read is L1-gated
	// (unlike W3C DID resolution). It is also the cross-registry fetch a peer
	// resolver calls; how that peer authenticates is a deployment concern.
	ResolveVC(context.Context, *connect.Request[v1.ResolveVCRequest]) (*connect.Response[v1.ResolveVCResponse], error)
}

// NewVCResolverServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewVCResolverServiceHandler(svc VCResolverServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	vCResolverServiceMethods := v1.File_dplaax_vc_v1_vc_proto.Services().ByName("VCResolverService").Methods()
	vCResolverServiceStoreVCHandler := connect.NewUnaryHandler(
		VCResolverServiceStoreVCProcedure,
		svc.StoreVC,
		connect.WithSchema(vCResolverServiceMethods.ByName("StoreVC")),
		connect.WithHandlerOptions(opts...),
	)
	vCResolverServiceResolveVCHandler := connect.NewUnaryHandler(
		VCResolverServiceResolveVCProcedure,
		svc.ResolveVC,
		connect.WithSchema(vCResolverServiceMethods.ByName("ResolveVC")),
		connect.WithHandlerOptions(opts...),
	)
	return "/dplaax.vc.v1.VCResolverService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case VCResolverServiceStoreVCProcedure:
			vCResolverServiceStoreVCHandler.ServeHTTP(w, r)
		case VCResolverServiceResolveVCProcedure:
			vCResolverServiceResolveVCHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedVCResolverServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedVCResolverServiceHandler struct{}

func (UnimplementedVCResolverServiceHandler) StoreVC(context.Context, *connect.Request[v1.StoreVCRequest]) (*connect.Response[v1.StoreVCResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dplaax.vc.v1.VCResolverService.StoreVC is not implemented"))
}

func (UnimplementedVCResolverServiceHandler) ResolveVC(context.Context, *connect.Request[v1.ResolveVCRequest]) (*connect.Response[v1.ResolveVCResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dplaax.vc.v1.VCResolverService.ResolveVC is not implemented"))
}
