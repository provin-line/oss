// dplaax.signer.v1 is the KMS-model signing service: Ed25519 signatures where
// the private key never crosses the RPC boundary. Pipeline processes and peers
// hold DIDs, not keys; they call this service, and the registry process holds
// the key material (via the keystore) and returns only the signature.
//
// Contract shape (slice-5 D-s1): both RPCs are a thin transport of the committed
// crypto.Signer seam — they take the RAW bytes to sign and return the RAW
// Ed25519 signature. Output encoding is the caller's concern, not this service's:
// VC proofs are base58btc-multibase-encoded by the vc layer (vc.CreateProof does
// its own multibaseEncodeBase58 over these raw bytes), and L2 wire-signing uses
// the raw 64-byte signature directly. The service does not re-frame the input as
// a "sha256:<hex>" prehash or the output as multibase — that would force a
// pointless decode round-trip on the client.
//
// Two RPCs, two domains (D-s2/D-s3): Sign and SignRaw are cryptographically
// identical (Ed25519 over the supplied bytes), but they are split so the two
// signing domains carry distinct authorization policies (an operator can grant
// VC-signing without granting wire-signing) and so each binds to its key
// relationship: Sign uses the DID's #signing assertionMethod key (VC proof
// domain), SignRaw uses the #auth authentication key (L2 wire domain). The
// server rejects a key_id that does not match the RPC's domain.
//
// This service is the KMS *deployment pattern* over the seam, not a protocol
// mandate: a single-process deployment may sign inline (crypto.Signer over local
// keys) and never expose this service, and still produce protocol-valid proofs.
// The proto standardizes the wire shape if you run one.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: dplaax/signer/v1/signer.proto

package signerpbconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/provin-line/oss/gen/go/dplaax/signer/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SignerServiceName is the fully-qualified name of the SignerService service.
	SignerServiceName = "dplaax.signer.v1.SignerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SignerServiceSignProcedure is the fully-qualified name of the SignerService's Sign RPC.
	SignerServiceSignProcedure = "/dplaax.signer.v1.SignerService/Sign"
	// SignerServiceSignRawProcedure is the fully-qualified name of the SignerService's SignRaw RPC.
	SignerServiceSignRawProcedure = "/dplaax.signer.v1.SignerService/SignRaw"
)

// SignerServiceClient is a client for the dplaax.signer.v1.SignerService service.
type SignerServiceClient interface {
	// Sign produces an Ed25519 signature over data with the DID's #signing
	// assertionMethod key — the VC proof-creation domain. data is the raw bytes to
	// sign (e.g. the proof hashData vc.CreateProof builds); the returned signature
	// is raw (the vc layer encodes it base58btc-multibase). key_id must be
	// "signing"; any other value is rejected (D-s3).
	Sign(context.Context, *connect.Request[v1.SignRequest]) (*connect.Response[v1.SignResponse], error)
	// SignRaw produces an Ed25519 signature over data with the DID's #auth
	// authentication key — the L2 wire-signing domain (chainmanager wireauth).
	// data is the raw bytes to sign (e.g. the JCS-canonicalized AuthProof view);
	// the returned signature is the raw 64-byte Ed25519 signature. key_id must be
	// "auth"; any other value is rejected (D-s3).
	SignRaw(context.Context, *connect.Request[v1.SignRawRequest]) (*connect.Response[v1.SignRawResponse], error)
}

// NewSignerServiceClient constructs a client for the dplaax.signer.v1.SignerService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSignerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SignerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	signerServiceMethods := v1.File_dplaax_signer_v1_signer_proto.Services().ByName("SignerService").Methods()
	return &signerServiceClient{
		sign: connect.NewClient[v1.SignRequest, v1.SignResponse](
			httpClient,
			baseURL+SignerServiceSignProcedure,
			connect.WithSchema(signerServiceMethods.ByName("Sign")),
			connect.WithClientOptions(opts...),
		),
		signRaw: connect.NewClient[v1.SignRawRequest, v1.SignRawResponse](
			httpClient,
			baseURL+SignerServiceSignRawProcedure,
			connect.WithSchema(signerServiceMethods.ByName("SignRaw")),
			connect.WithClientOptions(opts...),
		),
	}
}

// signerServiceClient implements SignerServiceClient.
type signerServiceClient struct {
	sign    *connect.Client[v1.SignRequest, v1.SignResponse]
	signRaw *connect.Client[v1.SignRawRequest, v1.SignRawResponse]
}

// Sign calls dplaax.signer.v1.SignerService.Sign.
func (c *signerServiceClient) Sign(ctx context.Context, req *connect.Request[v1.SignRequest]) (*connect.Response[v1.SignResponse], error) {
	return c.sign.CallUnary(ctx, req)
}

// SignRaw calls dplaax.signer.v1.SignerService.SignRaw.
func (c *signerServiceClient) SignRaw(ctx context.Context, req *connect.Request[v1.SignRawRequest]) (*connect.Response[v1.SignRawResponse], error) {
	return c.signRaw.CallUnary(ctx, req)
}

// SignerServiceHandler is an implementation of the dplaax.signer.v1.SignerService service.
type SignerServiceHandler interface {
	// Sign produces an Ed25519 signature over data with the DID's #signing
	// assertionMethod key — the VC proof-creation domain. data is the raw bytes to
	// sign (e.g. the proof hashData vc.CreateProof builds); the returned signature
	// is raw (the vc layer encodes it base58btc-multibase). key_id must be
	// "signing"; any other value is rejected (D-s3).
	Sign(context.Context, *connect.Request[v1.SignRequest]) (*connect.Response[v1.SignResponse], error)
	// SignRaw produces an Ed25519 signature over data with the DID's #auth
	// authentication key — the L2 wire-signing domain (chainmanager wireauth).
	// data is the raw bytes to sign (e.g. the JCS-canonicalized AuthProof view);
	// the returned signature is the raw 64-byte Ed25519 signature. key_id must be
	// "auth"; any other value is rejected (D-s3).
	SignRaw(context.Context, *connect.Request[v1.SignRawRequest]) (*connect.Response[v1.SignRawResponse], error)
}

// NewSignerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSignerServiceHandler(svc SignerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	signerServiceMethods := v1.File_dplaax_signer_v1_signer_proto.Services().ByName("SignerService").Methods()
	signerServiceSignHandler := connect.NewUnaryHandler(
		SignerServiceSignProcedure,
		svc.Sign,
		connect.WithSchema(signerServiceMethods.ByName("Sign")),
		connect.WithHandlerOptions(opts...),
	)
	signerServiceSignRawHandler := connect.NewUnaryHandler(
		SignerServiceSignRawProcedure,
		svc.SignRaw,
		connect.WithSchema(signerServiceMethods.ByName("SignRaw")),
		connect.WithHandlerOptions(opts...),
	)
	return "/dplaax.signer.v1.SignerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SignerServiceSignProcedure:
			signerServiceSignHandler.ServeHTTP(w, r)
		case SignerServiceSignRawProcedure:
			signerServiceSignRawHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSignerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSignerServiceHandler struct{}

func (UnimplementedSignerServiceHandler) Sign(context.Context, *connect.Request[v1.SignRequest]) (*connect.Response[v1.SignResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dplaax.signer.v1.SignerService.Sign is not implemented"))
}

func (UnimplementedSignerServiceHandler) SignRaw(context.Context, *connect.Request[v1.SignRawRequest]) (*connect.Response[v1.SignRawResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dplaax.signer.v1.SignerService.SignRaw is not implemented"))
}
