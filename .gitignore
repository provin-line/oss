# build artifacts
dist/

# OS / editor noise
.DS_Store
*.bak
*.swp

# stray local build artifacts (go build ./deploy/quickstart/provision, ./cmd/provin)
/provision
/provin

# machine-local agent working directory (audit reports, scratch); the tracked
# AI guidance is AGENTS.md + CLAUDE.md at the repo root, never here.
.claude/
