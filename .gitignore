# build artifacts
dist/
# stray `go build` output of cmd/standalone at repo root (binary artifact)
/standalone

# OS / editor noise
.DS_Store
*.bak
*.swp

# stray local build artifacts (go build ./deploy/quickstart/provision, ./cmd/provin)
/provision
/provin
