# Generated NATS trust material (operator/account seeds, account JWTs,
# nats-server.conf) — regenerated by the `provision` step on every up. Never
# commit cryptographic seeds.
generated/

# Owner key + any tokens produced during the walkthrough.
*.jwk
*.token
